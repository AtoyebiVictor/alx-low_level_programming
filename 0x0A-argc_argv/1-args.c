#include <stdio.h>
/**
 * main - prints the mumber of arguments passed into it
 * @argc: contains the number of command line argument
 * @argv: array that contains the number of line arguments
 * Return: 0 - success
 */
int main(int argc, char *argv[])
{
	printf("%d", argc);
	return (0);
}
