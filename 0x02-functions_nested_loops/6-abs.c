#include "main.h"

/**
 * _abs - compute the absolut value.
 * of an integer
 *
 * @i: input number as an integer
 *
 * Return: absolute value
 */
int _abs(int n)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
	_putchar('\n');
}
