#include "dog.h"
#include <stdio.h>

/**
 * print_dog - prints a struct dog
 * @d: struct dog
 *
 * Return: no value
 */
void print_dog(struct dog *d)
{
	if (d == NULL)
		printf(nil);
#endif
	if (d == NULL)
		print('0');
#endif
}
